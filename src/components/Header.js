import React from 'react';
import './Header.css';  // Importing Header component styles

const Header = () => {
  return (
    <header>
      <h1>Task Manager</h1>
    </header>
  );
};

export default Header;
